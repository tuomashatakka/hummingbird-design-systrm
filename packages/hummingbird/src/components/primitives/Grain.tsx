/**
 * A fixed, full-bleed procedural noise texture — the identity kit's
 * atmosphere layer. Mount once, near the root. `div[data-grain]`.
 */
export const Grain = () =>
  <div data-grain='' aria-hidden='true' />

Grain.displayName = 'Grain'
